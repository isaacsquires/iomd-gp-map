version: '3'

services:
  iomd-gp-map:
    container_name: iomd-gp-map
    ports:
      - '5000:5000'
    environment:
      - MAPBOX_ACCESS_TOKEN=${MAPBOX_ACCESS_TOKEN}
      - PORT=5000
    env_file:
      - .env
    build: .
